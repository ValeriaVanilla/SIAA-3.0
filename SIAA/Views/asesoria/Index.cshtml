@model IEnumerable<SIAA.asesoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CicloEscolar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.asesor.Correo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_horario.Dia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_lugar.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_unidad_aprendizaje.NombreUnidadAprendizaje)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CicloEscolar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.asesor.Correo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_horario.Dia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_lugar.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_unidad_aprendizaje.NombreUnidadAprendizaje)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAsesoria }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAsesoria }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAsesoria })
        </td>
    </tr>
}

</table>
